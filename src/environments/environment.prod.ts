/* eslint-disable @typescript-eslint/naming-convention */
import { version } from '../../package.json';

export const environment = {
    production: true,
    api_url: 'http://gpdi-hmg.norteenergiasa.com.br/api',
    version,
};
